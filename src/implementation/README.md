# Implementation Modules

This folder is reserved for additional app features that go beyond the core playback, tray, and volume plumbing.

Current modules:

- `settings_dialog.*` - tray-opened settings window for Spotify connection details.
